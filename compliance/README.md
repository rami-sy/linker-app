# Compliance documents

Operational and security attestation documents for platform reviews (Meta, hosting audits, etc.).

These files describe **process controls**, not runtime application code. Keep them in a private repository when they contain infrastructure details (IPs, SSH paths, etc.).

Do not commit secrets, credentials, or live API keys here.
